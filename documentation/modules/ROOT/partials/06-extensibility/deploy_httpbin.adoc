= Deploy httpbin

Deploy the application in your namespace:

[.console-input]
[source,bash,subs="+macros,+attributes"]
----
oc process -f httpbin/httpbin.yaml \
    -p HTTPBIN_ROUTE="${OCP_HTTPBIN_HOST}" -n ${OCP_NAMESPACE} \
    | oc apply -n ${OCP_NAMESPACE} -f -
----

Wait some seconds for all the pods to be ready:

[.console-input]
[source,bash,subs="+macros,+attributes"]
----
watch oc get pods -n ${OCP_NAMESPACE}
----

Verify that the application is reachable and is running ok:

[.console-input]
[source,bash,subs="+macros,+attributes"]
----
curl -s -kI "https://${OCP_HTTPBIN_HOST}"
----

.httpbin service working
image::06-extensibility/httpbin_curl.png[httpbin service working,width="80%",align="center",link=../_images/06-extensibility/httpbin_curl.png,window=_blank]

.httpbin service working
image::06-extensibility/httpbin_curl_01.png[httpbin service working,width="80%",align="center",link=../_images/06-extensibility/httpbin_curl_01.png,window=_blank]
