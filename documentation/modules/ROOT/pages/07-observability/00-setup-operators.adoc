
= Lab 1 - Install Tempo Operator

Naturally, we could install operators using the OpenShift Web Console, but for this tutorial, we will use the CLI.


image::07-observability/tempo_operator_01.png[link=../_images/07-observability/tempo_operator_01.png,window=_blank,width=75%]

image::07-observability/tempo_operator_02.png[link=../_images/07-observability/tempo_operator_02.png,window=_blank,width=75%]


[sidebar]
---
// https://github.com/alvarolop/quarkus-observability-app/tree/main/gitops/ocp-dist-tracing/01-tempo-operator

Navigate to the project folder in your system after repository download for completing all tasks included in this tutorial:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
cd ocp-servicemesh-labs-2025/lab-00/tempo
----


Create the `openshift-tempo-operator` namespace:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 10-ns-tempo-operator.yaml
----

Create an Operator group:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 20-operatorgroup-tempo.yaml
----

Create the subscription:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 30-sub-tempo.yaml
----

.How to know the current version of an operator.
[.lines_space]
[.console-input]
[%collapsible]
====
Before to create subscrption, if you want to know the specific channel name, currentCSV, and other information about this operator, run the following command:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc get packagemanifest tempo-product -o yaml
----

We highlight the lines:

[.lines_space]
[.console-input]
[source,yaml, subs="+macros,+attributes"]
----
.../...
status:
  catalogSource: redhat-operators
  catalogSourceDisplayName: Red Hat Operators
  catalogSourceNamespace: openshift-marketplace
  catalogSourcePublisher: Red Hat
  channels:
  - currentCSV: tempo-operator.v0.15.4-1
.../...
----
====

Check the Operator status by running the following command:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc get csv -n openshift-tempo-operator
----

[.lines_space]
[source,bash, subs="+macros,+attributes"]
----
oc get csv -n openshift-tempo-operator | grep tempo
tempo-operator.v0.15.4-1     Tempo Operator        0.15.4-1   tempo-operator.v0.15.3-1       Succeeded
----


= Lab 2 - Install Red Hat build of OpenTelemetry Operator

// https://github.com/alvarolop/quarkus-observability-app/tree/main/gitops/ocp-dist-tracing/02-opentelemetry-operator

Naturally, we could install operators using the OpenShift Web Console, but for this tutorial, we will use the CLI.

image::07-observability/opentelemetry_operator_01.png[link=../_images/07-observability/opentelemetry_operator_01.png,window=_blank,width=75%]

image::07-observability/opentelemetry_operator_02.png[link=../_images/07-observability/opentelemetry_operator_02.png,window=_blank,width=75%]

image::07-observability/opentelemetry_operator_03.png[link=../_images/07-observability/opentelemetry_operator_03.png,window=_blank,width=75%]


[sidebar]
---


Change directory to the `lab-00/03-coo` folder:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
cd ocp-servicemesh-labs-2025/lab-00/03-coo
----



Create the `openshift-opentelemetry-operator` namespace:


[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 12-ns-opentelemetry-operator.yaml
----

Create an Operator group:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 21-operatorgroup-opentelemetry-operator.yaml
----

Create the subscritpion:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 31-sub-opentelemetry.yaml
----


Check the Operator status by running the following command:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc get csv -n openshift-opentelemetry-operator
----

[.lines_space]
[source,bash, subs="+macros,+attributes"]
----
oc get csv -n openshift-opentelemetry-operator | grep "opentelemetry"
opentelemetry-operator.v0.119.0-2   Red Hat build of OpenTelemetry      0.119.0-2   opentelemetry-operator.v0.119.0-1      Succeeded
----


= Lab 3 - Install Cluster Observability Operator

// Source: https://github.com/alvarolop/quarkus-observability-app/tree/main/gitops/ocp-coo/

Naturally, we could install operators using the OpenShift Web Console, but for this tutorial, we will use the CLI.

image::07-observability/cluster-observability-operator_01.png[link=../_images/07-observability/cluster-observability-operator_01.png,window=_blank,width=75%]

image::07-observability/cluster-observability-operator_02.png[link=../_images/07-observability/cluster-observability-operator_02.png,window=_blank,width=75%]

image::07-observability/cluster-observability-operator_03.png[link=../_images/07-observability/cluster-observability-operator_03.png,window=_blank,width=75%]


[sidebar]
---



Change directory to the `lab-00/03-coo` folder:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
cd ocp-servicemesh-labs-2025/lab-00/03-coo
----


Create the subscription:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 20-sub-cluster-observability-operator.yaml
----


Check the Operator status by running the following command:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc get csv -n openshift-operators
----

[.lines_space]
[source,bash, subs="+macros,+attributes"]
----
oc get csv -n openshift-operators | grep "cluster-observability"
cluster-observability-operator.v1.1.1   Cluster Observability Operator       1.1.1
cluster-observability-operator.v1.1.0     Succeeded
----



We install the distributedTracing plugin for the *Cluster Observability Operator*:

[.lines_space]
[.console-input]
[source,bash, subs="+macros,+attributes"]
----
oc apply -f 30-uiplugin-distributedTracing.yaml
----



After the installation process the *OpenShift console web* shows the following message:

image::07-observability/uiplugin_tracing_01.png[link=../_images/07-observability/uiplugin_tracing_01.png,window=_blank,width=75%]

We refresh the web console and we can see the new plugin in the *Obverve* section:

image::07-observability/uiplugin_tracing_02.png[link=../_images/07-observability/uiplugin_tracing_02.png,window=_blank,width=75%]

And select a *Tempo Instance*:

image::07-observability/uiplugin_tracing_03.png[link=../_images/07-observability/uiplugin_tracing_03.png,window=_blank,width=75%]


image::07-observability/uiplugin_tracing_04.png[link=../_images/07-observability/uiplugin_tracing_04.png,window=_blank,width=75%]

// EOF